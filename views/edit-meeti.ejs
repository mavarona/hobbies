<main class="contenedor contenedor-formularios">
    <h1><%- namePage %></h1>
    <form class="default-form" method="POST">
        <legend>Sobre el Meeti</legend>
        <div class="campo">
            <label>Grupo</label>
            <select name="groupId">
                    <option value="" disabled selected>-- Selecciona Grupo --</option>
                    <% if(groups.length){ %>
                        <% groups.forEach( group => { %>
                            <option <%= (meeti.groupId === group.id) ? 'selected': '' %> value="<%= group.id %>"><%= group.name %></option>
                        <% }) %>
                    <% } %>
            </select>
        </div>
        <div class="campo">
            <label>Titulo</label>
            <input type="text" name="title" value="<%= meeti.title %>">
        </div>
        <div class="campo">
            <label>Invitado</label>
            <input type="text" name="invited" value="<%= meeti.invited %>">
        </div>
        <div class="columnas-2 grid">
            <div class="campo">
                <label>Fecha</label>
                <input type="date" name="date" value="<%= meeti.date %>">
            </div>
            <div class="campo">
                <label>Hora</label>
                <input type="time" name="hour" value="<%= meeti.hour %>">
            </div>
        </div>
        <div class="campo">
            <label>Cupo</label>
            <input type="number" min="1" name="cupo" value="<%= meeti.cupo %>">
        </div>
        <div class="campo descripcion">
            <label>Descripción</label>
            <div class="contenedor-editor">
                <input type="hidden" id="x" name="description" value="<%= meeti.description %>">
                <trix-editor input="x"></trix-editor>
            </div>
        </div>

        <legend>Ubicación Meeti</legend>

        <div class="campo buscador">
            <label>Coloca la Dirección del Meeti</label>
            <div class="contenedor-input">
                <input type="text" id="formSearch" placeholder="Ubicación">
            </div>
        </div>
        <small class="textSmall">El asistente colocará una dirección estimada</small>
        <hr>
        <div class="campo">
            <div id="mapid"></div>
        </div>
        <p class="informacion">Mueve el Pin hacía el punto exacto en caso de ser necesario</p>

        <p class="informacion">Confirma que los siguientes campos sean correctos:</p>
        <div class="campo">
            <label>Dirección</label>
            <input type="text" id="address" name="address" value="<%= meeti.address %>">
        </div>
        <div class="campo">
            <label>Ciudad</label>
            <input type="text" id="city" name="city" value="<%= meeti.city %>">
        </div>
        <div class="campo">
            <label>Estado</label>
            <input type="text" id="state" name="state" value="<%= meeti.state %>">
        </div>
        <div class="campo">
            <label>País</label>
            <input type="text" id="country" name="country" value="<%= meeti.country %>">
        </div>
        <div class="campo">
            <label>Latitud</label>
            <input type="text" id="lat" name="lat" readonly="readonly" value="<%= meeti.geo.coordinates[0] %>">
        </div>
        <div class="campo">
            <label>Longitud</label>
            <input type="text" id="lng" name="lng" readonly="readonly" value="<%= meeti.geo.coordinates[1] %>">
        </div>
        <div class="campo enviar">
            <input type="submit" class="btn btn-rosa" value="Guardar Cambios">
        </div>
    </form>
</main>
